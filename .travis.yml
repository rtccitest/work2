language: c

jobs:
  include:
    - stage: test
      matrix:
        include:
	  env:
            - TEST_CALL="FIRST CALL"
            - TEST_CALL="SECOND CALL"
            - TEST_CALL="THIRD CALL"
	script: gcc -c main.c && echo $TEST_CALL	
    - stage: deploy
      script: echo "Deploy"